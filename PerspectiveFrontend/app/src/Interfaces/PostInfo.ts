export default interface PostInfo {
    postId: string,
    header: string,
    timePosted: string,
    isVisible: boolean,
    authorName: string,
    rawHtml: string,
    slug: string,
    imageLocation: string
}